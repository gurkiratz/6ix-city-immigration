# Supabase project used by the immigration accelerator (admin + client intake).
NEXT_PUBLIC_SUPABASE_URL=
IMM_SECRET=

# Used by the browser + middleware for Supabase Auth (admin login).
# Safe to expose — publishable/anon key, not the secret key above.
NEXT_PUBLIC_SUPABASE_ANON_KEY=
